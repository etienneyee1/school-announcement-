# Application Structure

This folder groups the **backend** and **frontend** directories for the EduAnnounce full‑stack project.

- `backend/` – Express API server with JSON‑file persistence.
- `frontend/` – Vue 3 application scaffolded with Vite.

You can run both parts concurrently from the project root using `npm run dev`.
